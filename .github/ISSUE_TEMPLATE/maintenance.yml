name: "Maintenance"
description: "メンテナンス・リファクタリングタスク"
title: "[Chore] "
labels: ["maintenance"]
body:
  - type: textarea
    id: task
    attributes:
      label: "タスク内容"
      description: "何をしたいか簡潔に"
      placeholder: "例: Dependabotの未対応PRを整理したい"
    validations:
      required: true

  - type: dropdown
    id: type
    attributes:
      label: "種類"
      options:
        - "依存関係の更新"
        - "リファクタリング"
        - "CI/CD改善"
        - "ドキュメント"
        - "テスト追加"
        - "セキュリティ対応"
        - "その他"
    validations:
      required: true

  - type: textarea
    id: notes
    attributes:
      label: "補足（任意）"
    validations:
      required: false

  - type: markdown
    attributes:
      value: |
        ---
        **Claude Code でタスクを実行するには（GitHub Mobile 推奨手順）:**
        1. このIssueを作成する
        2. ラベル欄から `approved` を選択 → Claude が自動で作業開始 🚀

        コメントで指示する場合は `@claude [指示内容]` と書いてください。
        （iOS では `@claude` が自動大文字補正されることがあります。補正されたらバックスペースで直してください）
